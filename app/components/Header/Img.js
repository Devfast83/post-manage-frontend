import styled from 'styled-components';

import NormalImg from 'components/Img';

const Img = styled(NormalImg)`
  width: auto;
  margin: 0 auto;
  display: block;
  height: 300px;
`;

export default Img;
